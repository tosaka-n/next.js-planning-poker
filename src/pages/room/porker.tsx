import {
  Box,
  Button,
  Center,
  Flex,
  HStack,
  Text,
  keyframes,
  usePrefersReducedMotion,
} from "@chakra-ui/react";
import React, { useState } from "react";
import { Layout } from "src/components/Layout";

const Room = () => {
  return (
    <Layout>
      <Flex justifyItems={"center"}>
        <HStack spacing={"2rem"} m={"auto"} mt={"4rem"}>

        </HStack>
      </Flex>
    </Layout>
  );
};

export default Room;
